import React from 'react';
import ReactDOM from 'react-dom/client';
import WorkoutApp from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WorkoutApp />
  </React.StrictMode>
);
